{
    "en": {
        "region": {
            "register": {
                "errorType": {
                    "badRequest": "Register query must has keys: 'name', 'email', 'passwd', 'lang'.",
                    "forbidden": "User with email: '${email}' exists, choose another for registration."
                }
            },
            "auth": {
                "errorType": {
                    "badRequest": "Auth query must has keys: 'email', 'passwd'.",
                    "forbidden": "Email (${email}) or password (${passwd}) are wrong."
                }
            },
            "adverts": {
                "errorType": {
                    "badRequest": "Query must has 'token' property.",
                    "forbidden": "Invalid AUTH_TOKEN:\n${token}"
                }
            }
        }
    },
    "es": {
        "region": {
            "register": {
                "errorType": {
                    "badRequest": "La consulta de registro debe tener las claves: 'name', 'email', 'passwd', 'lang'.",
                    "forbidden": "Usuario con correo electrónico: '${email}' existe, elija otro para registrarse."
                }
            },
            "auth": {
                "errorType": {
                    "badRequest": "La consulta auth debe tener claves: 'email', 'passwd'.",
                    "forbidden": "El correo electrónico (${email}) o la contraseña (${passwd}) están incorrectos."
                }
            },
            "adverts": {
                "errorType": {
                    "badRequest": "La consulta debe tener propiedad 'token'.",
                    "forbidden": "No válido AUTH_TOKEN:\n${token}"
                }
            }
        }
    }
}
